# TEXTUS — Homiletikai műhely (Streamlit)
# ≥1.59: st.tabs DOM baseweb → react-aria; Gyorseszközök CSS dual-selector (ui_theme).
streamlit>=1.42.0
Authlib>=1.3.2
supabase
requests>=2.31.0
# Homiletikai profil radar chart (opcionális; hiánya esetén natív sáv-fallback)
plotly>=5.18.0
# Vázlat Word (.docx) export
python-docx>=1.1.0
